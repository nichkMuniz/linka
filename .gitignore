# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.config/
.env
.env.*
!.env.example
node_modules/
dist/
*.log
*.db
linka

# Migrações (docs/migrations, supabase/migrations) SÃO versionadas: são a
# fonte de verdade das policies RLS. Ignore apenas dumps locais do banco.
*.dump
*.sqldump

# Imagens de IA dos exercícios: material de trabalho pesado (centenas de MB).
# As imagens que o app usa vêm do Supabase Storage (bucket "exercises"),
# NÃO do repositório. Ficam só no disco local.
ai-exercise-images/

# Documentos/mockups de trabalho — não fazem parte do app em runtime.
mockup-metas.html
workouts-duplicados.md
exercise-image-prompts.md
